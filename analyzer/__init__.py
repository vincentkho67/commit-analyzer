"""commit-analyzer — pull a developer's GitHub commits over a time window and
produce a markdown report on code quality and estimated active hours.

Build order (see SPEC.md): fetch -> cluster -> analyze -> report -> cli.
"""

__version__ = "0.1.0"
